# Configuración de la aplicación
SECRET_KEY=tu_clave_secreta_super_segura
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Base de datos
# Para desarrollo local (SQLite se usa automáticamente si no se especifica DATABASE_URL)
# DATABASE_URL=sqlite:///./app.db
# Para producción (PostgreSQL en Render)
# DATABASE_URL=postgresql://user:pass@host:port/basededatos

# Orígenes permitidos para CORS (separados por coma, sin espacios)
FRONTEND_URL=http://localhost:5173,https://nexus-communicator.netlify.app